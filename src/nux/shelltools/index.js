import nux from "../base.js"

export const watch = nux.importScript(`${import.meta.dirname}/watch`)
export const watchfs = nux.importScript(`${import.meta.dirname}/watchfs`)

export default {
	watch,
	watchfs,
} 





